[project]
name = "volatile-bandit"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "wasmtime>=41.0.0",
    "rich>=13.0.0",
]

[tool.setuptools]
py-modules = ["main", "policy"]
